<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.deezerapirecyclerview">

    <uses-permission android:name="android.permission.INTERNET"/>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.DeezerAPIRecyclerView">
        <activity
            android:name=".vue.MainActivity"
            android:label="@string/app_name"
            android:theme="@style/Theme.DeezerAPIRecyclerView.NoActionBar">
        </activity>
        <activity android:name=".vue.MainPlaylist"
            android:theme="@style/Theme.DeezerAPIRecyclerView.NoActionBar"/>
        <activity android:name=".vue.MainMusic"
            android:theme="@style/Theme.DeezerAPIRecyclerView.NoActionBar"/>
        <activity
            android:name=".vue.SplashScreen"
            android:label="@string/app_name"
            android:screenOrientation="portrait"
            android:theme="@android:style/Theme.Black.NoTitleBar.Fullscreen"
            tools:ignore="Instantiatable">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>


    </application>

</manifest>